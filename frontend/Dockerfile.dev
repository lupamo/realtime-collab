FROM node:18-alpine

WORKDIR /app

# This is a placeholder - I'll replace this when building the Next.js app
# For now, just run a simple server that returns "Frontend Coming Soon"

RUN echo '{"name":"placeholder","version":"1.0.0"}' > package.json

EXPOSE 3001

CMD ["sh", "-c", "echo 'Frontend placeholder running on port 3001' && tail -f /dev/null"]